/**
 * SEARCH AUTO-SUGGEST
 * @author: Robert Morrison <r.w.morrison@stir.ac.uk>
 */

// we will add some new modules to the stir library
var stir = stir || {};

/**
 * Concierge
 * Instantiated below with `new stir.Concierge();`
 */
stir.Suggester = function Suggester(popup) {
  if (!stir.addSearch) return;
};
